\documentclass[./main.tex]{subfiles}
% \include{mathnotes}
% \include{cat_preamble}

\usetikzlibrary{decorations.pathreplacing}
\begin{document}
  \section{Introduction}
  \todo[inline]{summarize Nikolaus et. al}
  \todo[inline]{summarize Freed-Hopkins}
\section{Goals}
\begin{enumerate}
    \item define bundle theory  in general category a la nikolaus et al.
    \begin{enumerate}
        \item should require finite limits for now. \joseph{in particular, we need terminal object to define group objects amongst groupoids, unless we can use the general def from nlab later.} 
        %\joseph[inline]{Although this should be a harmless assumption. It's probably the case that this follows from natural requirements on "cech functor"
        % \(
        %  \C^{\to} \to \grpd{\C}
        % \). If not, we at least need terminal objects to define groups}
        \item settle on translation for ``has geometric realizations of internal groupoids" \joseph[inline]{(i.e. is left adjoint to discrete groupoid functor too strong? probably a philosophical question. how to proceed here? Note: this gives colimits of simplicial diagrams which arbitrary EHT's don't have a priori.)}
        \item translate ``cocycle info of groupoid category is preserved in $\C$": the previous requirement yields $ \grp{\C}\to \C^{\to}$ which should be fully faithful
    \end{enumerate}
    \item see if motivic space category satisfies these
    \item see if general elementary higher toposes satisfy them
\end{enumerate}

\end{document}
